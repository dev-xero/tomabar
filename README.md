# TomaBar

A local server and Android client to view TomatoBar Pomodoro session data remotely. 

The server reads the TomatoBar log file from the host machine and exposes it over a REST API. The Android app connects to the server and visualizes this data.
