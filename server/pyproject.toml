[project]
name = "server"
version = "0.1.0"
description = "TomatoBarRemote's web API."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "python-dotenv>=1.2.2",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
